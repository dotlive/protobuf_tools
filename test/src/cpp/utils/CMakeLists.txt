PROJECT(utils)

FILE(GLOB HEADER_UTILS "*.h")
FILE(GLOB SRC_UTILS "*.cpp")

SET(LIB_NAME utils)

INCLUDE_DIRECTORIES(${PBTOOL_ROOT}/protoc_3.6.x/include)
LINK_DIRECTORIES(${COMMON_OUTPUT}/${CMAKE_BUILD_TYPE})

ADD_LIBRARY(${LIB_NAME} ${SRC_UTILS})
