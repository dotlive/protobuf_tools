PROJECT(externals)

FILE(GLOB HEADER_EXTERNALS "rapidxml/*.h")
FILE(GLOB SRC_EXTERNALS "*.cpp")

SET(LIB_NAME externals)

INCLUDE_DIRECTORIES(${CMAKE_HOME_DIRECTORY}/externals/rapidxml)
LINK_DIRECTORIES(${COMMON_OUTPUT}/${CMAKE_BUILD_TYPE})

#ADD_LIBRARY(${LIB_NAME} ${SRC_EXTERNALS})
